import React from 'react';

/**
 * Header component
 */
const Header = () => {
  /**
   * Base CSS class
   */
  const baseCls = 'header';

  return (
    <div className={baseCls}>
      Album Index
    </div>
  );
};

export default Header;
